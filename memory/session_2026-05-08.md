# Session 2026-05-08

## Starting state

- Three confusing folders in `CYRUS/`: `cyrusthegreat`, `ctg_1`, `ctg_2` — user couldn't recall which was which
- `cyrusthegreat` `main` branch ahead of `origin/main` by 16 commits, 4 files uncommitted (Portal experimentation)
- No methodology files (`BRAIN.md` / `TODO.md` / `workflow_rules.md` / `tech_learnings.md`) in repo
- Multiple overlapping legacy docs from prior sessions (CURRENT_STATUS.md is December 2024 stale)
- Several other folders in `CYRUS/` the user had also forgotten about: `CTGANDRAILGUN` (35 GB), `contracts_quantum`, three pre-rename zip backups

## Goals for this session

- Disambiguate the three folders; figure out which is current, which is debug, which is deploy
- Restructure `CYRUS/` into a sensible layout
- Verify production state vs local state of cyrusthegreat
- Bootstrap the methodology files (BRAIN/TODO/workflow_rules/tech_learnings)

## Decisions / shipped / verified

### Folder structure (verified, applied)

```
CYRUS/
├── cyrusthegreat/       (unchanged, the real project)
├── tools/
│   ├── bank5-debug-ui/  (was: ctg_2 — debug UI for live Bank5 contract; pruned 3.1G → 7.1M)
│   └── hardhat-deploy/  (was: ctg_1/hardhat + deploy_contracts4.sh — only deploy pipeline)
├── CTGANDRAILGUN/       (35 GB, RailGun fork study — left alone, not yet investigated)
└── _archive/            (165 MB)
    ├── ctg_1-frontend-broken-scaffold/
    ├── ctg_1-backend-supabase-cache-api/
    ├── ctg_1-supabase-tokens-schema/
    ├── ctg_1-env-with-keys/
    ├── contracts_quantum-pqc-tee-experiment-aug14/
    └── old-snapshots-vaultwhisper-era/  (3 zips: 2025:08:13, 2025:08:23, cyrus-vault.zip)
```

Each archived folder has a `README.md` explaining provenance and whether it's safe to delete.

### Production-vs-local contract drift (VERIFIED)

- Verified by curling `https://cyrusthegreat.dev/assets/index-wFiv6NE4.js` and grepping for `0x[a-fA-F0-9]{40}`
- Live bundle calls **CrossChainBank5** at `0x3d6e43cbf157110015edF062173BbeBF78De61B4` (Sepolia)
- Local `main` branch's `.env` targets **CrossChainBank8** at `0xb83A814097C70DB79568b663662eA07e77D4D87a` (also Sepolia, deployed but not promoted)
- The `backup` branch (`cc2d992 v1.17.55`) is the source state matching what's live. Confirmed by user comparing http://localhost:5174 (backup branch with contract overridden to Bank5) side-by-side with https://cyrusthegreat.dev — UI matched.

### Solana status (VERIFIED unfinished)

- Anchor program written: `contracts/solana/lib.rs`, IDL, types, deploy keypair
- Frontend integration guide written: `contracts/solana/FRONTEND_INTEGRATION_GUIDE.md`
- BUT: zero React hooks for Solana, no wallet adapter, no UI, no tests, only testnet program ID in `.env`
- User confirmed: "I was working on Solana but didn't finish"

### Portal feature status (PARTIALLY verified)

- Contract: `contracts/evm/CyrusPortal11.sol` (1042 lines) — exists, deployment status unknown
- Frontend: `src/components/portal/PortalInterface.tsx` (597 lines), `src/components/portal/FloatingPortalWidget.tsx` (436 lines), `src/hooks/usePortal.ts` (648 lines) — exists
- Tests: NONE in `tests_evm/` cover Portal
- Documentation: NONE — Portal post-dates all the existing docs (CURRENT_STATUS.md, MASTER_PROJECT_DOCUMENTATION.md etc.)
- Uncommitted edits: fee-model rewrite to `commitDeposit` (removes per-commitment fee, +455/-40 lines across 3 files) — in progress

### macOS dev environment gotcha (RESOLVED, recorded as L-002)

- Default `node` on this machine is `/Applications/Codex.app/Contents/Resources/node` (bundled inside an app, hardened runtime + library validation)
- This node refuses to load Vercel-signed Next.js SWC native binary
- Fix: `nvm use 22.19.0` before any Next.js work
- Vite is fine because it uses esbuild, not SWC native

## Open threads for next session

- **(P0)** Decide what to do with the 16 unpushed Portal commits + 3 uncommitted edits — push & polish, squash, or abandon
- **(P0)** Decide whether to ship Bank8 to production (and how — what about users with balances in Bank5?)
- **(P0)** Add Portal tests before any Portal deploy
- **(P1)** Solana finish-or-drop decision
- **(P1)** Mainnet go/no-go decision (all chains currently `notdeployednow`)
- **(P1)** Fix `tools/hardhat-deploy/deploy_contracts4.sh` hardcoded paths (point at `cyrusthegreat/src/contracts/abis/`)
- **(P2)** Consolidate the 8 overlapping legacy docs into `BRAIN.md` + a slim `docs/` folder
- **(P3)** Investigate `CTGANDRAILGUN/` (35 GB) — likely contains the most recent thinking; separate session
- **(P3)** Look at `solana-reown-integration` branch — possibly the start of the Solana frontend integration

## Lessons learned this session (added to tech_learnings.md)

- **L-001**: Live deployment ≠ local source. Verify with curl + grep on the deployed bundle.
- **L-002**: macOS app-bundled node refuses third-party native modules. Use nvm.
- **L-003**: Audit capabilities separately on any v1→v2 migration. Don't trust "fully absorbed."
- **L-004**: Don't dismiss old folders by surface signals. Verify capability-by-capability.

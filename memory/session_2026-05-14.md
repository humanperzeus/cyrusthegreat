# Session 2026-05-14

(Continuation of the work that started late on 2026-05-13. New calendar date triggered this log.)

## Starting state

- Local main: 86 ahead / 61 behind `origin/main`. Methodology files + today's 8 commits sit on top of the long-standing Portal-experimentation stack.
- Decision from 2026-05-13: branch off `origin/main`, cherry-pick the meaningful subset of local main onto a clean stack, leave local main alone.
- Live site: cyrusthegreat.dev unchanged. Bank8 serving on all 3 chains; bundle `index-DG1Do22v.js`. Verified end-to-end via Rule 8 curl check at session start.

## Goals for this session

- Execute the divergence sort-out as a non-destructive add-only operation.
- Verify no secret leaks before considering any push (the branch may go to GitHub later).
- Start Phase B-2 implementation per the just-finalized spec: Session A = scaffold `CyrusTresor1.sol` from Bank8.

## Decisions / shipped / verified

### Divergence sort-out — completed

- Created branch `sync-2026-05-13` in a dedicated worktree at `.claude/worktrees/sync-2026-05-13/`, based on `origin/main` HEAD (`be8d247`). Main worktree untouched throughout.
- Cherry-picked 9 commits in chronological order:
  1. `832ef38` methodology scaffold (BRAIN.md, TODO.md, workflow_rules.md, tech_learnings.md, memory/, .gitignore additions)
  2. `d7f1f10` L-010 + session_2026-05-13 log
  3. `bc6fac1` debug UI moved into repo
  4. `a3e2b5f` BRAIN.md → 2026-05-13 state
  5. `ce9a440` workflow rules — backup branch demoted
  6. `262d4d8` session_2026-05-09 backfill
  7. `daa040a` .env.production deletion (no-op since origin/main never had it; kept via `--allow-empty` to preserve the "dashboard-only" policy commit message)
  8. `9db0ffd` CyrusTresor1 design spec
  9. `25b5b8f` session log extend
- Auto-merged `.gitignore` three times without conflict.
- Skipped: the 16 "pre 10 - N" / "pre 19 - 8" Portal-experiment commits + the `6571649` .env.production add (paired with its deletion in 8).
- **No actual conflicts** during cherry-pick.

### Secret-leak audit — clean

Ran a 5-check scan on the branch before considering push:

- Known rotated Alchemy key suffix (`<redacted-alchemy-suffix>`): not found anywhere.
- Wallet privkey prefixes (first 16 chars of WALLET1_PRIVK / WALLET2_PRIVK): not found.
- 64-char hex strings: matches only in EVM bytecode fragments inside ABI JSONs (public on-chain).
- Alchemy URLs with embedded keys: only matches in pre-existing `example.env` with placeholder `your_alchemy_api_key`.
- `API_KEY=` / `PRIVK=` / `secret=` patterns: zero.

Branch is safe to push when needed. User decision this session: keep local for now, no push.

### Phase B-2 Session A — `CyrusTresor1.sol` scaffold

- Copied `contracts/evm/CrossChainBank8.sol` → `.claude/worktrees/sync-2026-05-13/contracts/evm/CyrusTresor1.sol`.
- Replaced NatSpec header with v1-lineage note explaining: regular vault = Bank8 verbatim (preserved for compatibility), pool layer to be added in subsequent commits per `docs/cyrustresor1_spec.md`.
- Renamed contract identifier from `CrossChainBank8` to `CyrusTresor1` (2 occurrences in the file).
- No other code changes.

### Compile environment recovery — disposable /tmp env

- In-repo Solidity build environment is broken (`tools/hardhat-deploy/` has missing `node_modules`, no in-repo `hardhat.config.*`, the `deploy_contracts4.sh` paths point at deleted directories per the 2026-05-09 P1 list).
- Workaround: built a disposable compile env at `/tmp/cyrustresor-compile-check/` with `@openzeppelin/contracts@4.9.6` + `@chainlink/contracts@1.2.0` installed, compiled via global `solc 0.8.35` with `--include-path node_modules`.
- Bank8 baseline: compiled clean. CyrusTresor1: compiled clean, identical bytecode shape (only the contract name differs). One pre-existing NatSpec deprecation warning from inherited OZ assembly code — present in Bank8 baseline too.
- This `/tmp` env will be reused for the next compile verifications. NOT a permanent fix — the `tools/hardhat-deploy/` recovery is still a separate intent (P1 from 2026-05-09).

### Commit on `sync-2026-05-13` (10th commit)

- `5ac0b1c feat(contracts): scaffold CyrusTresor1.sol (= Bank8 regular vault, renamed)`
- Branch now 10 ahead of `origin/main`.

### Sessions B / C / D.1 / D.2 + multi-chain deploy (same day, later)

**Session B (commit `cdd57b6`)** — pool storage + `commitToPool()`. Added:
- `EPOCH_LENGTH = 3600`, immutable `zkVerifier` slot (v1 = address(0))
- `struct Commitment { uint64 depositEpoch; bool spent }` (one storage slot)
- `mapping(bytes32 => Commitment) commitments`, `mapping(address => uint256[]) poolBucketSizes`
- Constructor extended with `_poolTokens`, `_bucketSchedules`, `_zkVerifier`
- `event PoolDeposit` — intentionally NO `msg.sender`
- `commitToPool()` payable, wallet-only entry, fee at commit time, rate-limited

**Session C (commit `0a6ed14`)** — `revealFromPool()`:
- Recomputes commitment from preimage: `(secret, userSalt, withdrawTo, token, bucketIdx, address(this), block.chainid)`
- Reverts on: zk-mode, zero withdrawTo, unknown commitment, double-spend, same-epoch reveal
- CEI pattern: marks spent BEFORE external transfer
- No contract fee on reveal (pay-forward model — paid at commit)
- Renamed param `salt → userSalt` to avoid shadowing storage SALT immutable

**Session D.1 (commit `3f9f311`)** — hardhat-deploy recovery:
- Moved tools/hardhat-deploy/ into the repo
- `paths.root = "../../"` so hardhat treats repo root as project (HH1007 workaround)
- Solidity deps added to cyrusthegreat/package.json devDependencies
- All build artifacts gitignored, redirected under tools/hardhat-deploy/
- Deleted stale Bank4-era files (CrossChainBank4.sol, deployVault4.js, deploy_contracts4.sh)
- `npx hardhat compile` works under nvm 22.19.0

**Session D.2 (commits `c87b8b2` + `3915d8b`)** — deploy + verify scripts:
- `scripts/deployCyrusTresor1.ts` (per-chain configs, DRY_RUN mode, deployment-record JSON output)
- `scripts/verify.ts` (idempotent, BigInt-aware constructor arg restoration)
- `.env.example` template
- Fix: Etherscan API v2 single-key format (v1 deprecated May 31 2025)

**Multi-chain deploy + verification (commit `1b80b07`)** — per Rule 4 (canonical surface first):

| Network | Address | Verified |
|---|---|---|
| Sepolia | `0x223E25F961E29AaCc3dB49e5b00B30452D42c65e` | ✅ |
| BSC Testnet | `0xa2D2A04d6eE5887f20bF736E1d9014727d599F39` | ✅ |
| Base Sepolia | `0xc90610ce4DE152349932Af102650b6c9f8C6AD68` | ✅ |

Each preceded by DRY_RUN preview. Unique salt per deploy. Deployer = WALLET1_PUBK.
zkVerifier = address(0). Bucket schedules per spec § 5 testnet defaults.

Surprise win: the 2026-05-09 note "Etherscan v2 free tier doesn't cover BSC"
was about a different API path (token discovery). Source verification on
BSCScan via Etherscan v2 works fine.

### Env vars + Cloudflare sync

Added to `cyrusthegreat/.env` (main worktree, gitignored):
- `VITE_CTGTRESOR_ETH_TESTNET_CONTRACT` / `_BSC_` / `_BASE_` slots with new addresses
- Matching `_MAINNET_` slots = `notdeployednow`

`ctg-sync-env` pushed all 57 VITE_* vars to Cloudflare Pages. **Did NOT
trigger redeploy** — frontend doesn't reference VITE_CTGTRESOR_* yet
(that's Session F+). The vars are queued for next real frontend deploy.

### Live state at end of all this work

- `cyrusthegreat.dev`: UNCHANGED. Still serving the same bundle (`index-DG1Do22v.js`)
  with Bank8 references. CyrusTresor1 is fully deployed on-chain but NOT
  referenced by any live frontend code yet.
- Cloudflare dashboard: 57 env vars include the new CyrusTresor1 addresses.
- Local main: UNCHANGED (still 86 ahead / 61 behind origin/main).
- `sync-2026-05-13` branch: 17 commits ahead of origin/main, fully clean,
  local only.

## Open threads for next session

- **Phase B-2 Session E**: add CyrusTresor1 tab to `tools/contract-debug/index.html`.
  Test cards covering commitToPool happy path, revealFromPool happy path,
  MEV-redirect revert (tamper withdrawTo), same-epoch reveal revert,
  double-spend revert, bucket-out-of-range revert, ERC-20 path
  (USD1/WLFI). Verify on real Sepolia per Rule 2.
- **Phase B-2 Session F+**: frontend integration. New "Anonymity Pool"
  mode in `cyrusthegreat/src/`. Teleport URI generator (dapp composes
  `(secret, userSalt, withdrawTo, ...)` → shareable claim link).
  Recipient claim flow (paste URI → broadcast revealFromPool from any wallet).
- **Solidity test framework** (separate intent, recommended Foundry —
  `forge` already installed globally). Would add unit tests for
  commitToPool / revealFromPool revert paths BEFORE relying on debug-UI
  flows alone.
- **Phase B-2 Session D**: fix `tools/hardhat-deploy/` paths (this in-repo recovery is the P1 item) → deploy CyrusTresor1 to Sepolia.
- **Phase B-2 Session E**: add CyrusTresor1 tab to `tools/contract-debug/` with test cards. Verify on real Sepolia per Rule 2.
- **Phase B-2 Session F+**: frontend integration (Anonymity Pool mode + teleport URI flow).
- **Solidity test framework** (separate intent): the repo has no unit test framework. Could set up Hardhat or Foundry in-repo. Recommend Foundry — already installed globally (`forge 1.3.2`), faster, no node_modules sprawl.
- **`tools/hardhat-deploy/deploy_contracts4.sh`** (P1, 2026-05-09 carryover): hardcoded paths to deleted frontend/backend folders. Blocks any new deploy.
- **Env-var-sprawl refactor** (P1, 2026-05-09 carryover): 18 RPC URL vars → 2 + chain-slug map.
- **BSC token discovery alternative** (P2, 2026-05-09 carryover): Etherscan v2 free tier doesn't cover BSC.
- **Push `sync-2026-05-13`**: optional, no urgency. Branch is ready when needed.
- **Local main**: still 86 ahead / 61 behind. Untouched today. Sort-out option = once `sync-2026-05-13` lands on origin/main, reset local main to match.

## State at end of session (chronological, not yet committed in real-time terms)

- `sync-2026-05-13` branch: 10 commits ahead of `origin/main`, fully clean. Latest: CyrusTresor1 scaffold.
- Main worktree: still 86 ahead / 61 behind `origin/main`. Same dirty files as before (TODO.md, Portal sources). Not touched today.
- Live site: unchanged.
- Disposable compile env at `/tmp/cyrustresor-compile-check/` retained for next session.

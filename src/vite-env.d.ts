/// <reference types="vite/client" />

// Injected by vite.config.ts `define` at build time. SHA is the short
// git commit hash that produced this bundle; TIME is the ISO timestamp
// of the build. Both are baked in — no client-side git lookup.
declare const __BUILD_SHA__: string;
declare const __BUILD_TIME__: string;
